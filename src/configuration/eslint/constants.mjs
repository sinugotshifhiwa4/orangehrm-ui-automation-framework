export const FILE_GROUPS = {
  allTs: ["**/*.ts"],
  project: [
    "src/**/*.ts",
    "tests/**/*.ts",
    "fixtures/**/*.ts",
    "scripts/**/*.ts",
    "**/*.spec.ts",
  ],
  tests: ["tests/**/*.ts", "**/*.spec.ts"],
};
